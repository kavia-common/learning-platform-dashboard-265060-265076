# Base URL of lingua_backend (FastAPI), e.g. http://localhost:3001
# Must match where the backend is reachable from the browser.
# Example:
# NEXT_PUBLIC_API_BASE_URL=http://localhost:3001
NEXT_PUBLIC_API_BASE_URL=
